# Repo handoff — 2026-06-10

Written for the next Claude (or human) picking this up. Read end-to-end before doing anything.

## State right now

- **Branch:** `main` (no other local branches)
- **Local main is 27 commits ahead of `origin/main`.** Nothing on this branch has been pushed since the Sprint 1 work began — `git log origin/main..HEAD` shows the spec, the plan, the entire Sprint 1 execution arc, plus the +1-week PLAN slip commit at `ffd5343`. Pushing is a deliberate user decision (the repo is public on GitHub: `jumbomochi/ai-experiments`); do not push without explicit consent.
- **Uncommitted on `main`:** `PLAN.md` (the +1-week slip update from the original sprint plan — see `git diff PLAN.md`). The user has consciously left this uncommitted three times now. Commit only when asked.
- **Test suite:** 65 passing, ruff clean (`uv run pytest -q && uv run ruff check .`).
- **Worktrees:** none beyond the main checkout.

## Sprint 1 is done

The eval-substrate plan at `docs/superpowers/plans/2026-05-26-eval-substrate-sprint1.md` is fully implemented (commits `ca5ae1b..25404e8`, 24 commits). Substrate validated end-to-end by experiment `0001-inference-contract-validation` against `qwen2.5:0.5b-instruct` via Ollama on the Mac mini.

What got built:
- `migrations/001_init.sql` — 7-table Postgres schema with the immutability trigger on `gold_example`.
- `shared/db/` — connection helper + tiny SQL migration applier.
- `shared/inference/` — OpenAI-compatible HTTP client, 4-bucket error classification, exponential-backoff retry.
- `shared/models/` — pydantic `ModelManifest` + YAML registry + UPSERT into Postgres.
- `shared/goldsets/` — JSONL schema + Jinja2 renderer + idempotent loader.
- `shared/eval/cost/` — rate-card-driven pure-function cost accountant.
- `shared/eval/judges/` — deterministic scorer + aggregation + v0.1 bundle YAML (deterministic-only, lenient trust).
- `shared/eval/runner/` — preflight + campaign loop + budget enforcement + CLI.
- `experiments/0001-inference-contract-validation/` — the smoke run that proved it works.

## What needs doing — in priority order

### 0. Housekeeping (5 min)

- [ ] `git diff PLAN.md` and decide whether to commit the slip update. If yes: `git add PLAN.md && git commit -m "Update PLAN.md: +1-week slip (Sprint 1 actually started 2026-05-25)"`.
- [ ] Decide whether to push `main` to `origin`. The repo is public so this publishes everything — including the 24 Sprint-1 commits. The user has not yet been comfortable doing this; don't assume.

### 1. Pre-Sprint-2 cleanups (from the final code review — captured in memory)

These were intentionally deferred but should land before Sprint 2 starts adding new modules on top:

1. **Drop the `uuid5` hashing on `gold_example.example_id`.** Right now the PK is `uuid5(NAMESPACE_URL, "goldsets://" + readable_id)`, so the human-readable form `ex_general_seed0001` is unrecoverable from the schema. Joins are awkward (see `experiments/0001-inference-contract-validation/README.md` "Known follow-ups"). Cleanest fix: change the column to `text` and use the readable string as PK. New migration `002_gold_example_readable_id.sql`. Loader (`shared/goldsets/loader.py:_example_uuid`) becomes a no-op.

2. **Move the privacy guardrail from `runner.py` to `shared/inference/`.** The spec says "guardrail in `shared/inference/`, checked at adapter boundary" but the implementation puts it in the runner. No effect today (Tier 1 only on Mac), but Sprint 2 will add Tier-2 endpoints (Fireworks/Together/Groq) and the architectural intent is *adapter refuses the call*, not *runner refuses to dispatch*. Move before the first Tier-2 endpoint lands.

3. **Eliminate the double bundle-fetch.** `_check_trust_gate` fetches the judge bundle, then immediately afterward `_fetch_bundle()` fetches it again at `shared/eval/runner/runner.py:61,85`. Return the bundle out of preflight or thread it through.

4. **`halted_judge_uncalibrated` taxonomy.** There's a `# TODO(S3)` in `runner.py` at the `trust_gate` catch site. Sprint 3 (strict trust gate) will need to map `PreflightFailure("trust_gate", ...)` to `status="halted_judge_uncalibrated"` instead of `"halted_setup"`. Don't lose the comment.

5. **`tests/integration/` is an empty directory.** Either populate (move `tests/shared/eval/test_runner.py` there — it's already an integration test) or delete. Currently a navigation confuser.

6. **Runner couples to bundle JSONB shape at 4 call sites.** `bundle["judges"]["deterministic"]["config"]`, `bundle["aggregation"]["weights"]`, `bundle["trust"]["enforcement"]` etc. A `JudgeBundleView` adapter on `shared/eval/judges/` would concentrate that coupling. Worth tackling when Sprint 3 evolves the bundle schema for specialist/generalist judges.

7. **Re-run experiment 0001 against a real model.** The 0.5B Qwen answered "A" to every multi-choice question — substrate validated, model quality not. Once Spark is up, re-run `experiments/0001-inference-contract-validation/run.sh` after registering `qwen2.5:7b-instruct` (or 14b) as a model manifest. Don't change the seed; just point at a bigger model.

### 2. Sprint 2 — plan does not yet exist

When ready, the Sprint 2 plan should cover (per PLAN.md):

- **DGX Spark bring-up.** Per PLAN.md week 1 the Spark order was placed; ETA was ~2026-06-15 (i.e., this week). Verify whether it has actually arrived — there's no automated check, ask the user.
- **Blessed sovereign runtime on Spark.** NIM vs TRT-LLM vs vLLM bake-off; experiment `0002-inference-blessed-sovereign-runtime/`.
- **Per-target containers.** Same OpenAI-compat endpoint, different runtime per host.
- **Second model manifest** (a Spark-served model, e.g. Qwen 7B or 14B).
- **`ai-experiments-goldsets` private repo creation.** This is a one-time setup; the loader (`shared/goldsets/loader.py`) already expects a local path to a private repo. The repo doesn't exist on GitHub yet — needs creating.
- **Cost-discipline scaffold.** Budget per campaign with auto-teardown when on-demand boxes come online (cloud-burst target).
- **Gold-set v0.1 milestone.** Per PLAN.md the curation work starts in earnest in Sprint 2 (~150 examples).

Use the planning skills: `superpowers:brainstorming` then `superpowers:writing-plans`.

### 3. Phase 1 acceptance criteria — what's still open

Per `ROADMAP.md` §"Phase 1":
- ✅ Same gold-set example scored against Mac (done in experiment 0001)
- ⏳ Same against Spark (Sprint 2)
- ⏳ Same against measured GCE/EC2/Runpod (Sprint 2 cloud-burst target)
- ✅ A randomly chosen past run can be fully reconstructed from stored state (replay primitive — schema supports it; not yet exercised end-to-end as a test)
- ✅ Every eval run records its cost (`result.cost_increment_usd` + `run.cost_actual_usd`)
- ⏳ Campaigns exceeding `max_cost_usd` halt and tear down on-demand compute (halt path implemented; teardown hook is a `LocalTeardownHook` no-op — needs a real cloud-burst implementation in Sprint 2)

## Critical environment realities the next dev needs

- **Postgres:** Using the existing **PG17** at `/Library/PostgreSQL/17/` (EnterpriseDB-style installer), **not** brew `postgresql@16` as some plan text suggests. The plan's `/opt/homebrew/opt/postgresql@16/bin/...` references are historical — when the Sprint 1 work started, port 5432 was already taken by PG17 so we adopted it.
- **DB auth:** Local **trust** for the `huiliang` role, but **Unix socket only** (`local all huiliang trust` in `pg_hba.conf`). TCP loopback still requires a password. The `.env` file uses `?host=/tmp` to force socket connection:
  ```
  DATABASE_URL=postgresql://huiliang@/ai_experiments?host=/tmp
  DATABASE_URL_TEST=postgresql://huiliang@/ai_experiments_test?host=/tmp
  ```
  `.env` is gitignored — the next dev needs to recreate it. The bootstrap is reproducible via `sudo /Users/huiliang/GitHub/ai-experiments/scripts/setup_pg17_trust.sh` (idempotent).
- **pgvector:** **Not** installed for PG17. Deferred to Phase 5 (memory adapter). Don't add it to migration files; install it on-demand when memory work begins (compile from source against PG17's `pg_config` — see `docs/notes/postgres-setup.md` for the recipe).
- **Ollama:** 0.15.1, started via `brew services start ollama`. `qwen2.5:0.5b-instruct` is pulled. The OpenAI-compatible endpoint is at `http://localhost:11434/v1`. Manifest is registered in `model_manifest` table.
- **Judge bundle v0.1:** registered in `judge_config` table. Deterministic-only routing; lenient trust enforcement. The `TBD` strings in `judges.specialist.model_id` / `judges.generalist.model_id` are deliberate Sprint-1 markers — Sprint 3 fills them when real LLM judges arrive.

## Key files to read first

| Purpose | Path |
|---|---|
| The evaluation-system spec (4 hard-to-reverse decisions) | `docs/superpowers/specs/2026-05-14-evaluation-system-design.md` |
| Sprint 1 plan (done) | `docs/superpowers/plans/2026-05-26-eval-substrate-sprint1.md` |
| Sprint 1 smoke-run writeup | `experiments/0001-inference-contract-validation/README.md` |
| Postgres setup details | `docs/notes/postgres-setup.md` |
| Postgres trust-auth bootstrap | `scripts/setup_pg17_trust.sh` |
| The substrate roadmap | `ROADMAP.md` |
| Execution calendar with sprint windows | `PLAN.md` |

## Claude memory pointer

The previous Claude session saved a comprehensive project memory at:

```
/Users/huiliang/.claude/projects/-Users-huiliang-GitHub-ai-experiments/memory/
├── MEMORY.md                                    # one-line index
└── 2026-05-31-eval-substrate-sprint1-shipped.md # detailed state at Sprint 1 ship
```

That memory has commit-level details, the subagent-driven-development workflow notes (what worked, what didn't), and the same deferred-followups list with more colour. **If the new Claude account is on the same OS user (`huiliang`), this should still be readable.** If it's a different OS user, the memory is orphaned — this handoff doc is then the only source of truth, which is why it's checked into the repo.

## Two non-obvious gotchas

1. **Don't add `CREATE EXTENSION vector;` anywhere.** Several plan amendments through Sprint 1 removed this from test fixtures because pgvector isn't installed against PG17. If a new migration or test re-introduces it, things break confusingly. The convention is documented in `docs/notes/postgres-setup.md`.

2. **The "(1M context)" string in the `Co-Authored-By` footer is intentional.** Every Sprint 1 commit uses it; spec reviewers occasionally hallucinated it as a violation. It matches the user's chosen attribution style. Don't strip it.
